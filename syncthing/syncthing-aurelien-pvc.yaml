apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: syncthing-aurelien-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: my-longhorn
  resources:
    requests:
      storage: 1Gi
