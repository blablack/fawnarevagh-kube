apiVersion: batch/v1
kind: CronJob
metadata:
  name: kublicity-clean-raspio
spec:
  schedule: "0 4 1 * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        metadata:
          labels:
            app: kublicity
        spec:
          volumes:
            - name: storage-localpath-pvc
              persistentVolumeClaim:
                claimName: storage-localpath-pvc
            - name: nasio-nfs-pvc
              persistentVolumeClaim:
                claimName: nasio-nfs-pvc
          containers:
            - image: nucio.nowhere:30038/kublicity:latest
              imagePullPolicy: Always
              command: ["duplicity"]
              args:
                [
                  "remove-all-but-n-full",
                  "3",
                  "file:///backup/DejaDupRaspio",
                  "--force",
                ]
              name: kublicity-clean-raspio
              volumeMounts:
                - mountPath: "/data"
                  name: storage-localpath-pvc
                - mountPath: "/backup"
                  subPath: "MyBackup"
                  name: nasio-nfs-pvc
                - mountPath: "/root"
                  subPath: "kublicity"
                  name: storage-localpath-pvc
          nodeSelector:
            cputype: arm
          restartPolicy: Never
          dnsPolicy: "None"
          dnsConfig:
            nameservers:
              - 192.168.2.201
