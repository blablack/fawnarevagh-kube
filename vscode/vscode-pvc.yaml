apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vscode-server-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: my-longhorn
  resources:
    requests:
      storage: 5Gi