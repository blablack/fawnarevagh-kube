# docker-builder

## docker

Build image and push to local registry

### build image
```bash
docker build -t docker-builder .
```


### tag and push to local registry
```bash
docker tag kublicity:latest nucio.nowhere:30038/docker-builder:latest
docker push nucio.nowhere:30038/docker-builder:latest
```